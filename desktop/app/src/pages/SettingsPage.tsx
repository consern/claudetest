export function SettingsPage() {
  return (
    <div className="layout">
      <div className="panel list">
        <h3>Settings</h3>
        <div className="muted">Phase 1 keeps settings minimal.</div>
        <div className="muted">Provider keys and policies still come from backend env.</div>
      </div>
    </div>
  );
}

